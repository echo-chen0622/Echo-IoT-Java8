version: '3.0'

services:
  tb-js-executor:
    env_file:
      - queue-pubsub.env
  tb-core1:
    env_file:
      - queue-pubsub.env
  tb-core2:
    env_file:
      - queue-pubsub.env
  tb-rule-engine1:
    env_file:
      - queue-pubsub.env
  tb-rule-engine2:
    env_file:
      - queue-pubsub.env
  tb-mqtt-transport1:
    env_file:
      - queue-pubsub.env
  tb-mqtt-transport2:
    env_file:
      - queue-pubsub.env
  tb-http-transport1:
    env_file:
      - queue-pubsub.env
  tb-http-transport2:
    env_file:
      - queue-pubsub.env
  tb-coap-transport:
    env_file:
      - queue-pubsub.env
  tb-lwm2m-transport:
    env_file:
      - queue-pubsub.env
  tb-snmp-transport:
    env_file:
      - queue-pubsub.env
  tb-vc-executor1:
    env_file:
      - queue-pubsub.env
  tb-vc-executor2:
    env_file:
      - queue-pubsub.env
